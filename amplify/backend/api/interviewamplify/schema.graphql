type Interview @model @aws_cognito_user_pools{
	id: ID!
	title: String!
	profileInterviewsId: ID!
	createdAt: AWSDateTime!
	updatedAt: AWSDateTime!
}

type Message @model @aws_cognito_user_pools{
	id: ID!
	sender: String!
	messageInterviewId: ID!
	message: String!
	direction: String
	createdAt: AWSDateTime!
	updatedAt: AWSDateTime!
}

type Profile @model @aws_cognito_user_pools{
	id: ID!
	name: String!
	createdAt: AWSDateTime!
	updatedAt: AWSDateTime!
}
